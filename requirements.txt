# Qt preview shell (M2+); legacy tray/drag helpers retained until final cutover.
PySide6>=6.8,<7

# 运行完整功能（含系统托盘、托盘图标；直接拖入面板为实验功能）：
pystray>=0.19,<1
Pillow>=10,<12
windnd>=1.0,<2

# 核心逻辑仍只用 Python 标准库 + tkinter。
# 未安装 pystray/pillow：无托盘。
# windnd 默认不启用；如需测试直接拖入，设置 DESKTOP_TIDY_ENABLE_WINDND=1。
